comments:
  allowedFields: ["category", "email", "name", "message"]
  branch: "master"
  commitMessage: "Added a comment"
  filename: "{@timestamp}"
  format: "yml"
  moderation: false
  name: "chpmnrssll.github.io"
  path: "_data/comments/{options.slug}"
  requiredFields: ["email", "name", "message"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
  transforms:
    email: md5
