comments:
  allowedFields: [ "email", "message", "name" ]
  branch: "master"
  commitMessage: "Add comment {@id}"
  filename: "{@id}"
  format: "json"
  moderation: false
  name: "chpmnrssll.github.io"
  path: "_data/comments/{options.slug}"
  requiredFields: [ "email", "message", "name" ]
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"

  transforms:
    email: md5
