comments:
  allowedFields: ["category", "email", "name", "message", "text"]
  branch: "master"
  commitMessage: "Added a comment"
  filename: "{@timestamp}-{@id}"
  format: "yml"
  moderation: false
  name: "chpmnrssll.github.io"
  path: "_data/comments/{options.slug}"
  requiredFields: ["email", "name", "text"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
  transforms:
    email: md5
