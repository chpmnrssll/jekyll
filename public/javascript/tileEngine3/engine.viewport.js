engine.viewport = {};engine.viewport.x = 0;engine.viewport.y = 0;engine.viewport.oldX = 0;engine.viewport.oldY = 0;engine.viewport.scroll = function(xDir, yDir) {	engine.viewport.oldX = engine.viewport.x;	engine.viewport.oldY = engine.viewport.y;	engine.viewport.x += xDir;	engine.viewport.y += yDir;	engine.viewport.x = Math.max(engine.viewport.x, 0);	engine.viewport.y = Math.max(engine.viewport.y, 0);	engine.viewport.x = Math.min(engine.viewport.x, (engine.map.width-engine.screen.tilesPerRow)*engine.tile.spriteSheet.tileWidth);	engine.viewport.y = Math.min(engine.viewport.y, (engine.map.height-engine.screen.tilesPerCol)*engine.tile.spriteSheet.tileHeight);};engine.viewport.set = function(x, y) {	engine.viewport.oldX = engine.viewport.x;	engine.viewport.oldY = engine.viewport.y;	engine.viewport.x = x;	engine.viewport.y = y;	engine.viewport.x = Math.max(engine.viewport.x, 0);	engine.viewport.y = Math.max(engine.viewport.y, 0);	engine.viewport.x = Math.min(engine.viewport.x, (engine.map.width-engine.screen.tilesPerRow)*engine.tile.spriteSheet.tileWidth);	engine.viewport.y = Math.min(engine.viewport.y, (engine.map.height-engine.screen.tilesPerCol)*engine.tile.spriteSheet.tileHeight);};